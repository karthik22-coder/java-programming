import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        int speed = new Scanner(System.in).nextInt();
        System.out.println((speed > 100) ? "Speed exceeds 100 km/h" : "Speed within limit");
    }
}
