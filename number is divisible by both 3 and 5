import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        int num = new Scanner(System.in).nextInt();
        System.out.println((num % 3 == 0 && num % 5 == 0) ? "Divisible by 3 and 5" : "Not divisible by both");
    }
}
